{
  "name": "uniopencourse",
  "private": true,
  "workspaces": [
    "apps/frontend",
    "apps/backend"
  ],
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"npm run start:dev -w backend\" \"npm run dev -w frontend\"",
    "format": "prettier --write ."
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ccUnicode/UniOpenCourse.git"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prettier": "^3.8.1"
  },
  "dependencies": {
    "@nestjs/mapped-types": "^2.1.0",
    "class-validator": "^0.14.0"
  }
}
